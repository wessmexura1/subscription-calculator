# Конструктор подписок

**«Сколько стоит твоя жизнь в месяц»** — приложение для отслеживания и анализа подписок.

![Screenshot](screenshot.png)

## 🎯 Возможности

### Отслеживание подписок
- Добавление подписок из готового каталога популярных сервисов
- Кастомные подписки для любых сервисов
- Автоматический расчёт месячной/годовой стоимости
- Учёт валюты, периода оплаты и типа плана

### Аналитика
- Общая сумма расходов в месяц и год
- Стоимость часа использования каждого сервиса
- Распределение расходов по категориям (диаграмма)
- Сравнительный анализ подписок

### Рекомендации
- Автоматическое определение "кандидатов на отключение"
- Подсветка подписок с низким использованием
- Расчёт потенциальной экономии
- Топ подписок с лучшим соотношением цена/качество

### Удобства
- Сохранение данных в браузере (localStorage)
- Экспорт/импорт данных в JSON
- Адаптивный дизайн
- Тёмная тема

## 🚀 Запуск

### Установка зависимостей

```bash
cd subscription-calculator
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для продакшена

```bash
npm run build
```

## 🛠 Технологии

- **React 18** + TypeScript
- **Vite** — сборщик
- **Framer Motion** — анимации
- **Recharts** — графики
- **Lucide React** — иконки
- **CSS Modules** — стилизация

## 📁 Структура проекта

```
src/
├── components/          # UI компоненты
│   ├── ui/             # Базовые компоненты (Button, Input, etc.)
│   ├── Header/         # Шапка приложения
│   ├── StatsCards/     # Карточки со статистикой
│   ├── SubscriptionForm/ # Форма добавления подписки
│   ├── SubscriptionTable/ # Таблица подписок
│   ├── Charts/         # Графики
│   └── Recommendations/ # Блок рекомендаций
├── data/               # Пресеты сервисов
├── hooks/              # React хуки
├── styles/             # Глобальные стили
├── types/              # TypeScript типы
├── utils/              # Утилиты и расчёты
├── App.tsx             # Главный компонент
└── main.tsx            # Точка входа
```

## 📊 Формулы расчёта

### Месячная стоимость
```
monthly_cost = price / billing_period_months
```

### Часы использования в месяц
```
hours_per_month = hours_per_week * 4.33
```

### Стоимость часа
```
cost_per_hour = monthly_cost / hours_per_month
```

### Оценка ценности (Value Score)
```
value_score = (importance * hours_per_month) / monthly_cost * 100
```

## 🎨 Дизайн

Приложение выполнено в строгом минималистичном стиле:
- Тёмная тема с оттенками серого
- Шрифт Manrope
- Плавные анимации
- Адаптивная сетка

